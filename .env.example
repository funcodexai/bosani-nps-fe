# Opsional jika sudah pakai public/api-config.js di server (disarankan untuk beda VM).
# Contoh: VITE_API_BASE=http://34.9.36.125
VITE_API_BASE=

# Catatan: prioritas URL API = api-config.js → VITE_API_BASE → path relatif /api
