# Dev: kosongkan agar /api di-proxy ke backend lokal (vite.config.ts).
# Production build: lihat .env.production (VITE_API_BASE backend GCP).
VITE_API_BASE=http://34.9.36.125


# Prioritas di runtime: public/api-config.js → VITE_API_BASE (build) → path relatif /api
